# tech-challenge-four

This repository contains the code and documentation for the Tech Challenge Four project.

## Running locally

1. Install make

    ```bash
    brew install make
    ```

2. Run the following command to start the application:

    ```bash
    make up
    ```

3. Access the application in your web browser at `http://localhost:8000`.
